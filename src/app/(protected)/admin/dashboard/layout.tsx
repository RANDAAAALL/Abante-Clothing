import { ReactNode } from "react";

// export const experimental_ppr = true;

export default function AdminDashboardLayoout({ children }: { children: ReactNode}){
    return <div>{children}</div>    
}